# 🎬 CastIt — איך להריץ את האפליקציה

## הפעלה מהירה (3 שלבים)

### 1. פתחי PowerShell
- לחיצה ימנית על כפתור התחל → **Windows PowerShell**
- או: Win+R → הקלידי `powershell` → Enter

### 2. נווטי לתיקיית הפרויקט
העתיקי והדביקי את הפקודה הזו ב-PowerShell ולחצי Enter:

```powershell
cd "C:\Users\yampe\OneDrive\Desktop\castit"
```

### 3. הריצי את האפליקציה
```powershell
npm run dev
```

המתיני כ-5 שניות עד שתראי:
```
✓ Ready in 2.3s
- Local:        http://localhost:3000
```

### 4. פתחי את הדפדפן
לכי לכתובת: **[http://localhost:3000](http://localhost:3000)**

---

## ⏹️ איך לעצור את האפליקציה

בחלון PowerShell שבו רץ השרת, לחצי **Ctrl+C** (פעם או פעמיים).

---

## 🔑 קבצי המפתחות (Environment Variables)

המפתחות שמורים בקובץ:
```
C:\Users\yampe\OneDrive\Desktop\castit\.env.local
```

הקובץ מכיל 3 מפתחות:
- `NEXT_PUBLIC_SUPABASE_URL` — כתובת ה-DB
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` — מפתח גישה ציבורי
- `ANTHROPIC_API_KEY` — מפתח Claude AI (ל-Aria)

**⚠️ חשוב:** אם תשני את הקובץ הזה, צריך לעצור (Ctrl+C) ולהפעיל מחדש (`npm run dev`).

---

## 🗂️ מה יש באפליקציה

| דף | מה זה |
|----|------|
| `/welcome` | מסך פתיחה |
| `/signup` | הרשמה (יוצרת משתמש אמיתי ב-Supabase) |
| `/verify` → `/typecast` → `/avatar` | תהליך onboarding |
| `/feed` | פיד וידאו בסגנון TikTok |
| `/discover` | חיפוש טאלנטים עם **Typecast filters** (Match %) |
| `/opportunities` | אתגרים יומיים + הזדמנויות קסטינג |
| `/messages` | רשימת DM + שיחות real-time |
| `/notifications` | התראות |
| `/studio/agent` | צ'אט עם **Aria** (AI אמיתי דרך Claude) |
| `/profile/me` | הפרופיל שלך + עריכה |

---

## 🛠️ פתרון בעיות

### "Port 3000 is already in use"
משהו אחר תופס את הפורט. הריצי ב-PowerShell:
```powershell
Get-NetTCPConnection -LocalPort 3000 | Select -First 1 | ForEach { Stop-Process -Id $_.OwningProcess -Force }
```

### "npm: command not found"
Node.js לא מותקן. הורידי מ-[nodejs.org](https://nodejs.org) → גרסת LTS.

### האפליקציה איטית להתחיל
הפעם הראשונה איטית (קומפילציה). הפעם השנייה תעלה תוך 2-3 שניות.

### "Module not found"
התלויות לא הותקנו. הריצי:
```powershell
cd "C:\Users\yampe\OneDrive\Desktop\castit"
npm install
```

### Aria לא עונה / לא מחוברת ל-AI
בדקי שה-`ANTHROPIC_API_KEY` ב-`.env.local` תקין ועצרי + הפעילי מחדש.

---

## 🗄️ Database (Supabase)

כתובת הפרויקט: [supabase.com/dashboard](https://supabase.com/dashboard)

- **Authentication → Users** — לראות משתמשים רשומים
- **Table Editor** — לראות נתונים בטבלאות (`profiles`, `messages`, `notifications` וכו')
- **SQL Editor** — להריץ queries ידנית

קובץ הסכמה: `supabase-schema.sql` בתיקיית הפרויקט.

---

## 📁 מבנה הפרויקט

```
castit/
├── app/                    # דפי Next.js
│   ├── (main)/             # דפים עם BottomNav
│   ├── api/aria/           # API של AI
│   ├── signup/, welcome/   # onboarding
│   └── ...
├── components/             # קומפוננטות UI
├── lib/
│   ├── supabase.ts         # חיבור ל-DB
│   ├── db.ts               # פונקציות שאילתה
│   ├── store.ts            # state גלובלי (Zustand)
│   └── mock-data.ts        # נתוני דמו (fallback)
├── .env.local              # מפתחות ⚠️ סודי
└── supabase-schema.sql     # סכמת DB
```

---

🎉 **בהצלחה!** השרת חי כל עוד החלון של PowerShell פתוח.
